

<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - ASP.NET Uygulamam</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top ">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @if (Session["username"] != null)
                {      <ul>
                    <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> @Session["username"]<span class="caret"></span></a>
                    <ul class="dropdown-menu">

                        
                        
                            <li><a href="/Home/MakaleADD?id=@Convert.ToUInt32(Session["ID"])">Makale ekle</a> </li>
                    </ul>

                </li>
                </ul>

                }
                else
                {
                    <a class="navbar-brand">Hakan özcan</a>}
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Anasayfa", "Index", "home") </li>
                    <li>@Html.ActionLink("hakkımda", "About", "home") </li>

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Konular <span class="caret"></span></a>
                        <ul class="dropdown-menu">

                            @foreach (var item in Model.Category)
                            {
                                <li><a href="/Home/makale?id=@item.ID">@item.Categoryname</a> </li>

                            }

                                <li><a href="#">c++</a></li>

                            


                        </ul>

                    </li>
                    <li>@Html.ActionLink("iletisim", "Contact", "home")</li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    @if (Session["username"] != null)
                    {
                        <li><a href="bloggers/cikis"><i class="fa fa-sign-out" > </i></a></li>
                    }
                    else
                    {
                        <li ><a href="/bloggers/Login"><i class="fa fa-sign-in " aria-hidden="true"></i></a></li>
                        <li>@Html.ActionLink("Üye ol", "Create", "bloggers")</li>
                    }





                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>


        <br />
         <br />
    <br />
        @RenderBody()
        <hr />
        <footer>
            <div class="row">
                <div class="col-lg-12">
                    <p>Copyright &copy; Your Website 2014</p>
                </div>
            </div>
            <!-- /.row -->
        </footer>
    

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
